gateway LB - tu mam najwiekszą zagwostke - nie mozna tutaj zrobic tego czysto jak w AWS.
architektura azure blokuje mozliwosc oddzielenia terraform od AKS pod moja architekture.

bez dokladania skomplikowanej architektury.
Najprostszym i najtanszym rozwiazaniem jest podzielenie etapu terraform na 2 kroki.
tworze architekture -> potem wdrazam klaster -> potem wdrazam frontdoor. Jednak wymaga to wiecej wysilku operacyjnego

moglbym wykorzystac najbardziej czysty i prosty wzorzec- ale Azure nie umozliwia takiego rozwiazania
ILB -> VMSS backend pool -> nodes -> cilium gateway nodeport
jednak ILB nie moze wskazac calej dostepnej puli IP podsieci, tylko konkretne, co ejst ograniczeniem Azure
Wskazanie konkretnych IP rozwiazuje problem, ale przy autoscalingu wystepuje problem

Kolejnym najprostszym sposobem wydaje sie przejsc z Nodeport na load balancer -> wtedy to AKS bedzie zarzadzal load balancer -> co wyeliminuje powyzszy problem.
Trade oof takiej decyzji bedzie taki, ze to AKS bedzie modyfikowal resource terraform, ale mozemy to obejsc poprzez lifecycle w terraform.
To wydaje sie najprostrzym podejsciem i takze najbardziej cost-optimized, jednoczesnie z najmniejszym narzutem operacyjnym
ALE wtedy mamy problm z zaleznoscia Front Door -> nie utworzymy go, poniewaz nie ma jeszcze load balancer...

Terraform moze stworzyc LB -> to eliminuje problem z Front Door.

Cilium musialo by tworzyc wewnetrzny lb ktory rozwiazuje problem z IP nodes
Ale wtedy mamy 2 LB. 

Kubernetes/AKS nie zapewnia prostego, natywnego mechanizmu, który pozwala usłudze LoadBalancer przejąć istniejący, dowolnie utworzony przez Terraform Azure Load Balancer jako własny managed load balancer

Najbardziej czyste, chodz drozsze rozwiazanie to uzycie App gateway + cilium gateway lb.
Roznica w kosztach jest ogromna - do kilkaset dolarow/msc

Jednak zrobienie 2 LB wydaje sie byc najmniej kosztowne - mozna sprobowac. Eliminuje problem z cilium gateway service:NodePort, gdzie trzeba na sztywno wpisac IP w terraform

Front Door -> stabilny IP terraform LB -> cilium gateway LB 

Wariant A

Front Door
   ↓
Application Gateway
   ↓
Cilium LB
   ↓
Gateway

vs 

Wariant B

Front Door
   ↓
Terraform ILB
   ↓
Cilium ILB
   ↓
Gateway

Wariand B jest **technicznie wspieralnym wzorcem** pod warunkiem że chaining jest w obsługiwanej konfiguracji.

I najwazniejsze -> mozemy wskazac konkretne IP w Cilium gateway LoadBalancer.

Wariant B daje mi:

brak Application Gateway,
brak ręcznego grzebania w VMSS,
brak hardcodowania IP node'ów,
AKS sam zarządza swoim LB/backendami,
Terraform może mieć stabilny LB potrzebny Front Door,
Front Door może być utworzony od początku,
mniej komponentów L7/L4,
dużo niższy koszt.

A Front Door Premium oficjalnie obsługuje Internal Load Balancer jako origin przez Private Link.

Backend musi być routowalny w ramach VNet (peering/routing wystarczy, nie musi być bezpośrednio ta sama subnet).
Health probes Twojego Terraform-LB będą uderzać w Cilium ILB, który sam robi swój health-check do podów - czyli masz dwie warstwy health-checkingu w szeregu. To działa, ale warto to jawnie zaznaczyć w dokumentacji jako świadomy trade-off (opóźnienie propagacji stanu unhealthy, potencjalnie 2x dłuższy failover).

"App Gateway WAF removed as redundant - Front Door Premium provides native WAF at edge"

Niestety nie mozna wykorzystac LB dla front door. Azure znow blokuje rozwiązania.

error:PrivateLinkServiceIsNotSupportedForIPBasedLoadBalancer: Private link service is not supported for load balancer with backend addresses set by (virtualNetwork, ipAddress) or (subnet, ipAddress).

pozostaje ostatnia metoda z app gateway.

[!IMPORTANT] Private link configuration support for tunneling traffic through private endpoints to Application Gateway is unsupported with private-only gateways.

Azure nie wspiera takiej mozliwosci. Jest jeszcze zacofana wzgledem AWS.
Nie mozna wykorzystac zadnej uslugi ktora polaczy private AKS z front door za 1 podejsciem

Azure ma ograniczenie: konfiguracja Private Link na Application Gateway do tunelowania przez Private Endpoint nie jest obsługiwana, gdy App Gateway ma wyłącznie private frontend IP.

musimy wystawic app gateway publicznie - wtedy placimy duzo za dodatkowe private polaczenie - juz lepiej wlaczyc WAF na app agteway

At the time of implementation, Azure did not support Private Link tunneling to a private-only Application Gateway. The architecture therefore used an alternative deployment model.


koszty:
Azure Front Door premium: 330$/msc
Application Gateway WAF v2: 330$/msc
Azure LB (standard) 18,25$/msc + 0,005$/GB
Internal LB 0$/msc (cilium gateway)
Azure private endpoint 7.3$/msc

Po 3 dniach wpadlem na pomysl - przeniesc tworzenie cilium gateway LB to bootstrap init i zrobic depends_on-boorstrap success. I potem wdrażanie dalej front door!
wtedy mamy wszysto najmniejszym kosztem.
Architektura stoi za 1 razem, najtansza opcja. Jedyny tradeoff to wieksza podatnosc na bledy, jesli bootstrap sie nie powiedzie - jednak jest to bardzo niewielki problem.



