controller:
  type: daemonset

  podLabels:
    app: alloy

  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 250m
      memory: 256Mi

alloy:
  configMap:
    create: true