replicaCount: 3

image:
  repository: rehox0/portfolio-backend
  tag: latest

serviceAccount:
  create: true
  name: backend
  roleArn: "allegro-analytics-eks-dev-backend-irsa-role"

env:
  debug: "True"
  allowedHosts: "*"

secret:
  name: backend-secret

service:
  type: ClusterIP
  port: 8000